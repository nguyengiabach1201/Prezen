{
    "name": "prezen",
    "version": "3.0.0",
    "description": "A professional markdown presentation tool",
    "type": "module",
    "main": "./src/prezen.js",
    "scripts": {
        "build": "bun build ./src/prezen.js --compile --minify --sourcemap --target bun"
    },
    "engines": {
        "bun": ">=1.0.0"
    },
    "dependencies": {
        "@agent-infra/browser-finder": "^0.2.2",
        "gray-matter": "^4.0.3",
        "marked": "^17.0.2",
        "pptxgenjs": "^4.0.1",
        "puppeteer-core": "^24.37.5"
    },
    "devDependencies": {
        "typescript": "^5.9.3"
    }
}
